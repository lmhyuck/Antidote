## 🚀 시작 가이드 (Getting Started)

이 프로젝트는 **OCR(광학 문자 인식)**과 **NLP(자연어 처리)** 기술을 활용하여 근로계약서의 독소 조항을 분석하고 수정안을 제안하는 RAG 기반 서비스입니다.

```bash

# 필수 패키지 설치
pip install -r requirements.txt

# 서버 실행
python -m app.main
# 서버 실행 시 DB 세팅 및 모델 로드까지 적용
```
