두 파일 전부 확인했습니다. 코드 기준으로 정확하게 나눠드릴게요.

---

# 전체 구조

```
src/
├── pages/
│   ├── Home.jsx              ← AIChatbotHome.jsx 에서
│   └── Analysis.jsx          ← LegalAIAssistant.jsx 에서
│
├── components/
│   ├── layout/
│   │   ├── Navbar.jsx
│   │   └── Sidebar.jsx
│   │
│   ├── search/
│   │   ├── SearchBar.jsx
│   │   └── QuickActions.jsx
│   │
│   ├── analysis/
│   │   ├── FindingsCard.jsx
│   │   ├── LiveAnalysis.jsx
│   │   ├── SuggestedTopics.jsx
│   │   └── CitationButtons.jsx
│   │
│   └── ui/
│       ├── Button.jsx
│       └── Spinner.jsx
│
├── styles/
│   └── global.css
│
└── App.js
```

---

## AIChatbotHome.jsx 에서 어디로 가는지

```
AIChatbotHome.jsx
│
├── <nav> 상단 로고 + 링크 + 벨 + 프로필
│    └── components/layout/Navbar.jsx
│        (profileImg import 제거, 벨/헬프 아이콘만 유지)
│
├── <aside> 왼쪽 사이드바
│    └── components/layout/Sidebar.jsx
│        (New Chat 버튼, Recent Chat/History 등 메뉴)
│
├── 검색창 input + 버튼
│    └── components/search/SearchBar.jsx
│        (FiSearch, navigate 로직 포함)
│
├── Quick Actions 버튼 4개
│    └── components/search/QuickActions.jsx
│        (Write a poem / Summarize text 등)
│
└── Feature Cards Grid (Multimodal, Private, Analytics, Developer)
     └── pages/Home.jsx 에 그대로 둠
         (재사용 안 하는 홈 전용 콘텐츠)
```

---

## LegalAIAssistant.jsx 에서 어디로 가는지

```
LegalAIAssistant.jsx
│
├── <header> 상단 로고 + 링크 + 벨 + Export 버튼
│    └── components/layout/Navbar.jsx
│        (AIChatbotHome nav와 합침, props로 버튼 다르게 처리)
│
├── <aside> 왼쪽 사이드바
│    └── components/layout/Sidebar.jsx
│        (Active Cases / Document Library 등 메뉴
│         + Upload New Contract 버튼)
│
├── Findings Card (border-l-4 파란선 카드)
│    └── components/analysis/FindingsCard.jsx
│        (CheckCircle2, AlertCircle 항목들)
│
├── 오른쪽 떠있는 Live Analysis 패널
│    └── components/analysis/LiveAnalysis.jsx
│        (RISK SCORE, CLAUSE CLARITY 바)
│
├── 오른쪽 떠있는 Suggested Topics 패널
│    └── components/analysis/SuggestedTopics.jsx
│        (Liability Limitations 등 리스트)
│
├── Citations 버튼 3개 (§14.2, Case #772-B 등)
│    └── components/analysis/CitationButtons.jsx
│        (Link2, History, ShieldCheck 아이콘)
│
├── Actions 버튼 (Fix this clause, Search Precedents, Explain Risk)
│    └── components/analysis/FindingsCard.jsx 하단에 포함
│        (같은 카드 안에 있는 액션이라 분리 안 함)
│
└── Floating Input Bar (Paperclip + input + Mic + Send)
     └── pages/Analysis.jsx 에 그대로 둠
         (이 페이지에서만 쓰는 고정 하단바)
```

---

## Navbar.jsx 처리 방법

두 파일의 nav/header가 비슷하게 생겼지만 버튼이 다릅니다.

```jsx
// props로 구분
<Navbar variant="home" />      ← 벨 + 헬프 아이콘
<Navbar variant="analysis" />  ← 벨 + 세팅 + Export 버튼
```

---

## Sidebar.jsx 처리 방법

두 파일의 사이드바 메뉴 내용이 다릅니다.

```jsx
// props로 메뉴 데이터만 다르게 넘김
<Sidebar items={homeMenuItems} />
<Sidebar items={analysisMenuItems} />
```

---

## global.css 로 가는 것

```
AIChatbotHome.css 에서
  → @import Manrope 폰트
  → :root 변수 (--primary 등)
  → 스크롤바
  → body 기본 설정

LegalAIAssistant.css 에서
  → @import Public Sans 폰트
  → :root 변수 (--dot-blue 등)
  → 스크롤바 (중복이라 하나만)
  → @keyframes fadeIn
  → button:active scale

각 컴포넌트 안에 남는 것
  → .glass-nav        → Navbar.jsx 안
  → .live-card        → LiveAnalysis.jsx 안
  → .panel-animate-in → FindingsCard.jsx 안
  → .bar-animate      → LiveAnalysis.jsx 안
```

---

## 작업 순서

```
1. global.css 정리
2. Navbar.jsx 분리 (두 파일에 공통)
3. Sidebar.jsx 분리 (두 파일에 공통)
4. SearchBar.jsx + QuickActions.jsx
5. FindingsCard.jsx + CitationButtons.jsx
6. LiveAnalysis.jsx + SuggestedTopics.jsx
7. pages/Home.jsx + pages/Analysis.jsx 최종 정리
```

두 파일 전부 확인했습니다. 코드 기준으로 정확하게 나눠드릴게요.

최종아래

---

# 전체 구조

```
src/
├── pages/
│   ├── Home.jsx              ← AIChatbotHome.jsx 에서
│   └── Analysis.jsx          ← LegalAIAssistant.jsx 에서
│
├── components/
│   ├── layout/
│   │   ├── Navbar.jsx
│   │   └── Sidebar.jsx
│   │
│   ├── search/
│   │   ├── SearchBar.jsx
│   │   └── QuickActions.jsx
│   │
│   ├── analysis/
│   │   ├── FindingsCard.jsx
│   │   ├── LiveAnalysis.jsx
│   │   ├── SuggestedTopics.jsx
│   │   └── CitationButtons.jsx
│   │
│   └── ui/
│       ├── Button.jsx
│       └── Spinner.jsx
│
├── styles/
│   └── global.css
│
└── App.js
```

---

## AIChatbotHome.jsx 에서 어디로 가는지

```
AIChatbotHome.jsx
│
├── <nav> 상단 로고 + 링크 + 벨 + 프로필
│    └── components/layout/Navbar.jsx
│        (profileImg import 제거, 벨/헬프 아이콘만 유지)
│
├── <aside> 왼쪽 사이드바
│    └── components/layout/Sidebar.jsx
│        (New Chat 버튼, Recent Chat/History 등 메뉴)
│
├── 검색창 input + 버튼
│    └── components/search/SearchBar.jsx
│        (FiSearch, navigate 로직 포함)
│
├── Quick Actions 버튼 4개
│    └── components/search/QuickActions.jsx
│        (Write a poem / Summarize text 등)
│
└── Feature Cards Grid (Multimodal, Private, Analytics, Developer)
     └── pages/Home.jsx 에 그대로 둠
         (재사용 안 하는 홈 전용 콘텐츠)
```

---

## LegalAIAssistant.jsx 에서 어디로 가는지

```
LegalAIAssistant.jsx
│
├── <header> 상단 로고 + 링크 + 벨 + Export 버튼
│    └── components/layout/Navbar.jsx
│        (AIChatbotHome nav와 합침, props로 버튼 다르게 처리)
│
├── <aside> 왼쪽 사이드바
│    └── components/layout/Sidebar.jsx
│        (Active Cases / Document Library 등 메뉴
│         + Upload New Contract 버튼)
│
├── Findings Card (border-l-4 파란선 카드)
│    └── components/analysis/FindingsCard.jsx
│        (CheckCircle2, AlertCircle 항목들)
│
├── 오른쪽 떠있는 Live Analysis 패널
│    └── components/analysis/LiveAnalysis.jsx
│        (RISK SCORE, CLAUSE CLARITY 바)
│
├── 오른쪽 떠있는 Suggested Topics 패널
│    └── components/analysis/SuggestedTopics.jsx
│        (Liability Limitations 등 리스트)
│
├── Citations 버튼 3개 (§14.2, Case #772-B 등)
│    └── components/analysis/CitationButtons.jsx
│        (Link2, History, ShieldCheck 아이콘)
│
├── Actions 버튼 (Fix this clause, Search Precedents, Explain Risk)
│    └── components/analysis/FindingsCard.jsx 하단에 포함
│        (같은 카드 안에 있는 액션이라 분리 안 함)
│
└── Floating Input Bar (Paperclip + input + Mic + Send)
     └── pages/Analysis.jsx 에 그대로 둠
         (이 페이지에서만 쓰는 고정 하단바)
```

---

## Navbar.jsx 처리 방법

두 파일의 nav/header가 비슷하게 생겼지만 버튼이 다릅니다.

```jsx
// props로 구분
<Navbar variant="home" />      ← 벨 + 헬프 아이콘
<Navbar variant="analysis" />  ← 벨 + 세팅 + Export 버튼
```

---

## Sidebar.jsx 처리 방법

두 파일의 사이드바 메뉴 내용이 다릅니다.

```jsx
// props로 메뉴 데이터만 다르게 넘김
<Sidebar items={homeMenuItems} />
<Sidebar items={analysisMenuItems} />
```

---

## global.css 로 가는 것

```
AIChatbotHome.css 에서
  → @import Manrope 폰트
  → :root 변수 (--primary 등)
  → 스크롤바
  → body 기본 설정

LegalAIAssistant.css 에서
  → @import Public Sans 폰트
  → :root 변수 (--dot-blue 등)
  → 스크롤바 (중복이라 하나만)
  → @keyframes fadeIn
  → button:active scale

각 컴포넌트 안에 남는 것
  → .glass-nav        → Navbar.jsx 안
  → .live-card        → LiveAnalysis.jsx 안
  → .panel-animate-in → FindingsCard.jsx 안
  → .bar-animate      → LiveAnalysis.jsx 안
```

---

## 작업 순서

```
1. global.css 정리
2. Navbar.jsx 분리 (두 파일에 공통)
3. Sidebar.jsx 분리 (두 파일에 공통)
4. SearchBar.jsx + QuickActions.jsx
5. FindingsCard.jsx + CitationButtons.jsx
6. LiveAnalysis.jsx + SuggestedTopics.jsx
7. pages/Home.jsx + pages/Analysis.jsx 최종 정리
```

파일 역할
Home.jsx 메인 홈 페이지 전체
Analysis.jsx법률 분석 페이지 전체
Navbar.jsx상단 네비게이션 바
Sidebar.jsx왼쪽 사이드바
SearchBar.jsx검색 input + 버튼
QuickActions.jsxWrite a poem 등 4개 버튼
AnalysisResultCard.jsx분석 결과 카드
LiveAnalysis.jsx오른쪽 실시간 분석 패널
SuggestedTopics.jsx오른쪽 추천 토픽 패널
CitationButtons.jsx§14.2 등 인용 버튼 3개
Button.jsx재사용 버튼 컴포넌트
Spinner.jsx로딩 스피너

import 찾는 법
규칙: 코드에서 쓰이는 것만 import
Analysis.jsx 코드를 위에서 아래로 읽으면서 찾습니다:

코드에서 쓰인 것 어디서 import
─────────────────────────────────────────
useState → react
Bot, Sparkles → lucide-react
History, Sliders → lucide-react
CheckCircle2,AlertCircle → lucide-react
Paperclip, Mic, Send → lucide-react
Navbar → ../components/layout/Navbar
Sidebar → ../components/layout/Sidebar
FindingsCard → ../components/analysis/FindingsCard
CitationButtons → ../components/analysis/CitationButtons

다음은 FindingsCard.jsx, LiveAnalysis.jsx, SuggestedTopics.jsx, CitationButtons.jsx 순서로 만들면 됩니다.

## 하드코딩된 것들 — 나중에 지울 것

### Analysis.jsx

```jsx
// 1. 하드코딩된 유저 메시지
<p className="text-sm leading-relaxed text-slate-700 text-left">
  제14.2조(편의에 의한 계약 해지)와 서비스 수준 부속서(SLA)와의
  관계를 고려하여 답변하시오.   // ← 삭제, API 응답으로 교체
</p>

// 2. 하드코딩된 AI 응답 텍스트
<p className="text-sm font-semibold text-slate-900 ...">
  제14.2조는 사전 통지 90일을...   // ← 삭제, API 응답으로 교체
</p>

// 3. 하드코딩된 케이스명
Analyzing V-24-901 Enterprise Agreement  // ← API에서 받아올 것
```

### FindingsCard.jsx

```jsx
// 4. 하드코딩된 분석 결과
<h4>Qualified Right to Terminate</h4>
<p>제14.2조는 사전 통지 90일을 조건으로...</p>

<h4>The 3-Month Trigger Risk</h4>
<p>해당 조항은 "3개월 연속"이라는 조건이...</p>
// ← 전부 삭제, AI 분석 결과로 교체
```

### LiveAnalysis.jsx

```jsx
// 5. 하드코딩된 수치
<span className="text-red-500">Elevated</span>
<div className="h-full bg-red-500 w-[85%]">  // ← 85% 하드코딩
<span className="text-blue-500">Optimal</span>
<div className="h-full bg-blue-500 w-[95%]"> // ← 95% 하드코딩
// ← AI 리스크 점수로 교체
```

### SuggestedTopics.jsx

```jsx
// 6. 하드코딩된 토픽 리스트
<li>• Liability Limitations</li>
<li>• Force Majeure Audit</li>
<li>• GDPR Compliance Gaps</li>
// ← AI 추천 토픽으로 교체
```

### CitationButtons.jsx

```jsx
// 7. 하드코딩된 인용 데이터
V-24-901 §14.2
Precedent: Case #772-B
Statutory Ref: UCC-301
// ← AI가 찾아온 실제 인용으로 교체
```

---

## 교체 방식 예시

```jsx
// 지금
<p>제14.2조는 사전 통지 90일을...</p>;

// 백엔드 연결 후
const [aiResponse, setAiResponse] = useState("");

<p>{aiResponse}</p>;
```

---

## 한 줄 요약

```
텍스트로 직접 박혀있는 것 전부 → 나중에 API 응답 데이터로 교체
```


## 프론트엔드 컴포넌트 분리 작업 요약

### 완성된 파일 구조
```
src/
├── pages/
│   ├── Home.jsx          ← AIChatbotHome.jsx 에서 분리
│   └── Analysis.jsx      ← LegalAIAssistant.jsx 에서 분리
│
├── components/
│   ├── layout/
│   │   └── Navbar.jsx    ← 두 페이지 공통, variant로 분기
│   │
│   ├── search/
│   │   ├── SearchBar.jsx
│   │   └── QuickActions.jsx
│   │
│   └── analysis/
│       ├── AnalysisResultCard.jsx  ← FindingsCard에서 rename
│       ├── LiveAnalysis.jsx
│       ├── SuggestedTopics.jsx
│       └── CitationButtons.jsx
│
├── styles/
│   └── global.css
│
└── App.js
```

---

### 작업 내용 요약

**global.css**
```
폰트 import
:root 변수
body 기본설정 + min-height
스크롤바
button:active
```

**Navbar.jsx**
```
variant="home"     → FiHelpCircle
variant="analysis" → FiSettings + Export Report 버튼
glass-nav CSS 포함
return() 누락 버그 수정
```

**Home.jsx**
```
feature-card CSS 포함
analytics-bar + @keyframes barGrow CSS 포함
lg:ml-64 제거 (Sidebar 없앰)
```

**Analysis.jsx**
```
Floating Input Bar → 상단 검색바로 이동
absolute 오른쪽 패널 → grid 3컬럼으로 변경
left-64 제거 (Sidebar 없앰)
Bot, Sparkles 아이콘 제거
```

**AnalysisResultCard.jsx**
```
findings-card, panel-animate-in CSS 포함
Actions 버튼 className 복구
```

**LiveAnalysis.jsx**
```
live-card, bar-animate CSS 포함
@keyframes growBar, slideInRight 포함
```

**SuggestedTopics.jsx**
```
live-card CSS 포함
```

---

### 제거된 것들
```
Sidebar.jsx        → 미사용으로 제거
LegalAIAssistant.jsx → Analysis.jsx로 대체
/chat 라우트       → /analysis로 변경
하드코딩 스타일    → Tailwind 또는 컴포넌트 내 style로 이동
```

---

### 남은 작업
```
백엔드 연결 시
→ 하드코딩 텍스트 전부 API 응답으로 교체
→ LiveAnalysis 수치 동적 처리
→ SuggestedTopics 동적 리스트
→ CitationButtons 동적 데이터
```