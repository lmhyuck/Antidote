## 프론트엔드 컴포넌트 분리 작업 요약

### 완성된 파일 구조
```
src/
├── pages/
│   ├── Home.jsx          ← AIChatbotHome.jsx 에서 분리
│   └── Analysis.jsx      ← LegalAIAssistant.jsx 에서 분리
│
├── components/
│   ├── layout/
│   │   └── Navbar.jsx    ← 두 페이지 공통, variant로 분기
│   │
│   ├── search/
│   │   ├── SearchBar.jsx
│   │   └── QuickActions.jsx
│   │
│   └── analysis/
│       ├── AnalysisResultCard.jsx  ← FindingsCard에서 rename
│       ├── LiveAnalysis.jsx
│       ├── SuggestedTopics.jsx
│       └── CitationButtons.jsx
│
├── styles/
│   └── global.css
│
└── App.js
```

---

### 작업 내용 요약

**global.css**
```
폰트 import
:root 변수
body 기본설정 + min-height
스크롤바
button:active
```

**Navbar.jsx**
```
variant="home"     → FiHelpCircle
variant="analysis" → FiSettings + Export Report 버튼
glass-nav CSS 포함
return() 누락 버그 수정
```

**Home.jsx**
```
feature-card CSS 포함
analytics-bar + @keyframes barGrow CSS 포함
lg:ml-64 제거 (Sidebar 없앰)
```

**Analysis.jsx**
```
Floating Input Bar → 상단 검색바로 이동
absolute 오른쪽 패널 → grid 3컬럼으로 변경
left-64 제거 (Sidebar 없앰)
Bot, Sparkles 아이콘 제거
```

**AnalysisResultCard.jsx**
```
findings-card, panel-animate-in CSS 포함
Actions 버튼 className 복구
```

**LiveAnalysis.jsx**
```
live-card, bar-animate CSS 포함
@keyframes growBar, slideInRight 포함
```

**SuggestedTopics.jsx**
```
live-card CSS 포함
```

---

### 제거된 것들
```
Sidebar.jsx        → 미사용으로 제거
LegalAIAssistant.jsx → Analysis.jsx로 대체
/chat 라우트       → /analysis로 변경
하드코딩 스타일    → Tailwind 또는 컴포넌트 내 style로 이동
```

---

### 남은 작업
```
백엔드 연결 시
→ 하드코딩 텍스트 전부 API 응답으로 교체
→ LiveAnalysis 수치 동적 처리
→ SuggestedTopics 동적 리스트
→ CitationButtons 동적 데이터
```